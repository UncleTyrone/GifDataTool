# Optional server-side defaults when running locally (`python-dotenv` loads this).
# The hosted app saves credentials per visitor in the browser (localStorage), not here.

ROBLOX_API_KEY=
ROBLOX_USER_ID=

# Optional — if set, uploads are owned by this group instead of the user.
# ROBLOX_GROUP_ID=

# Optional — default path to the Lua module to patch (same as the main text box).
# GIFDATA_LUA_PATH=

# Optional — set to 1 on Streamlit Community Cloud if the “save .env on server” panel still appears.
# GIFDATA_HIDE_DOTENV_SAVE=1
