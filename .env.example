# Copy to .env and fill in. Used by `python-dotenv` when running `streamlit run app.py`.

ROBLOX_API_KEY=
ROBLOX_USER_ID=

# Optional — if set, uploads are owned by this group instead of the user.
# ROBLOX_GROUP_ID=
